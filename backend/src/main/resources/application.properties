spring.application.name=backend

# SQLite datasource (development/testing)
# Add busy_timeout to handle concurrent access (30 seconds)
# date_string_format tells SQLite how to handle DATE columns
spring.datasource.url=jdbc:sqlite:backend.db?busy_timeout=30000&date_string_format=yyyy-MM-dd
spring.datasource.driver-class-name=org.sqlite.JDBC
# Configure connection pool for SQLite
spring.datasource.hikari.maximum-pool-size=1
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# Use Flyway for schema migrations (see db/migration/V1__init.sql)
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.jpa.hibernate.ddl-auto=none

# Let Flyway manage schema; disable Hibernate DDL auto
spring.sql.init.mode=never
spring.jpa.show-sql=true

spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
spring.jpa.properties.hibernate.boot.allow_jdbc_metadata_access=false
# Configure Hibernate for SQLite concurrency
spring.jpa.properties.hibernate.connection.provider_disables_autocommit=false

# Uploads
app.upload.dir=uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Email (configure in production)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nadabejaui@gmail.com
spring.mail.password=zcqqsnavatvagvpx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.mail.from=nadabejaui@gmail.com


logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
